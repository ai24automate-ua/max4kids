/**
 * CATALOG CONFIG
 * ---------------------------------------------------------------
 * Тут НЕ МАЄ БУТИ жодних API-ключів чи токенів.
 * Airtable PAT і доступ до Salesdrive YML обробляються ВИКЛЮЧНО
 * на бекенді (див. server/api-proxy-example.js). Фронтенд ходить
 * тільки на свої власні /api/* ендпоінти.
 *
 * Якщо колись виникне спокуса вписати сюди токен "тимчасово" —
 * не треба: він піде в git, в CDN-кеш, у DevTools будь-якого
 * відвідувача. Немає "тимчасово безпечно" для секретів у фронтенді.
 */
window.CATALOG_CONFIG = {
  // Бекенд-проксі, який сам ходить в Airtable з токеном у env
  AIRTABLE_ENDPOINT: '/api/products',

  // Бекенд-проксі, який сам качає та кешує YML-фід Salesdrive
  YML_ENDPOINT: '/api/feed',

  // Скільки тримати дані в sessionStorage, щоб не довбати ендпоінти
  // при кожному переході/ре-рендері
  CACHE_TTL_MS: 5 * 60 * 1000, // 5 хв

  /**
   * ЯК YML-офер вказує на "батьківську" модель з Airtable (parent_crm_code).
   *
   * 'name-contains' (дефолт, під реальну структуру Salesdrive/Airtable):
   *   parent_crm_code — це просте слово/фраза, яку менеджер вписує в
   *   Airtable (напр. "Мрія"), а в YML воно міститься десь усередині
   *   довгої назви офера (напр. "Санки-трансформер дитячі Мрія 2025
   *   колір: рожевий рейки-рожево-ванільні ТМ Veloz"). Матчинг — це
   *   пошук підрядка (без урахування регістру) у <name> офера.
   *
   *   ВАЖЛИВО: підрядковий збіг крихкий. Якщо два parent_crm_code
   *   частково перекриваються (напр. "Мрія" і "Мрія 2025"), офер
   *   може зматчитись під обидва — код бере найдовший збіг і пише
   *   попередження в консоль. Тримай коди моделей унікальними й
   *   достатньо специфічними, щоб такого не було.
   *
   * 'vendorCode-prefix': артикул виду "PATRIOT-RED" -> parent "PATRIOT".
   * 'param': окремий <param name="..."> з кодом моделі.
   */
  YML_MATCH_STRATEGY: 'name-contains', // 'name-contains' | 'vendorCode-prefix' | 'param'
  YML_MATCH_PARAM_NAME: 'Модель',          // використовується, якщо стратегія 'param'
  YML_VENDOR_CODE_SEPARATOR: '-',
  // Коди коротші за це — надто ризиковані для підрядкового пошуку
  // (легко випадково зматчаться всередині іншого слова)
  YML_MATCH_MIN_CODE_LENGTH: 3,

  // Фолбек-мапа кольорів на hex, якщо в YML немає готового коду кольору
  // (Salesdrive зазвичай віддає лише текстову назву кольору)
  COLOR_HEX_FALLBACK: {
    'червоний': '#D6483C',
    'синій': '#2E6E9E',
    'зелений': '#3E8E63',
    'жовтий': '#E0A83E',
    'сірий': '#7C8A94',
    'чорний': '#2B333A',
    'рожевий': '#E38FA5',
    'помаранчевий': '#E07A3E',
    'бірюзовий': '#5FB8AE',
    "м'ятний": '#5FB8AE',
    'білий': '#F3F6F8',
  },
  COLOR_HEX_DEFAULT: '#7C8A94',

  /**
   * Основи кольорів для фолбеку, коли назва офера НЕ містить патерну
   * "колір: XXX" (напр. "Санки пластикові SnowStar зелені" — колір
   * останнім словом без префікса). extractColorName() у
   * salesdrive-yml.js перевіряє, чи починається якесь слово назви з
   * цієї основи ("зелен" покриває "зелений"/"зелена"/"зелені") — це
   * не повна морфологія, але покриває типові відмінки прикметників
   * кольору. Значення — канонічний ключ, який далі шукається в
   * COLOR_HEX_FALLBACK вище.
   */
  COLOR_NAME_STEMS: {
    'червон': 'червоний',
    'син': 'синій',
    'зелен': 'зелений',
    'жовт': 'жовтий',
    'сір': 'сірий',
    'чорн': 'чорний',
    'рожев': 'рожевий',
    'помаранч': 'помаранчевий',
    'бірюзов': 'бірюзовий',
    "м'ятн": "м'ятний",
    'біл': 'білий',
  },
};
